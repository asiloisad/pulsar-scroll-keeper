{
  "name": "scroll-keeper",
  "description": "Try to keep your focus on the same point as you resize pane",
  "repository": "https://github.com/asiloisad/pulsar-scroll-keeper",
  "keywords": [
    "screen",
    "position"
  ],
  "version": "4.0.4",
  "license": "MIT",
  "engines": {
    "atom": ">=1.100.0 <2.0.0"
  },
  "main": "./lib/main",
  "configSchema": {
    "debounceSoftWrap": {
      "title": "Debounce Soft Wrap (ms)",
      "description": "Delay in milliseconds before recalculating soft-wrap after resize stops. Higher values reduce CPU usage during resize but delay the final wrap adjustment.",
      "type": "integer",
      "default": 150,
      "minimum": 0,
      "maximum": 1000
    }
  }
}
